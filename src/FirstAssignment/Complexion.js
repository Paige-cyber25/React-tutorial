import classes from './Complexion.module.css';

const Complexion = () => {
    return (
        <p className={classes.orange}>I am fair-skinned in complexion.</p>
    );
};

export default Complexion;